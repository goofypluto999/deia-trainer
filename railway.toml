[package]
name = "deia-trainer"
version = "0.0.0"
description = "Deia's Personal Trainer Backend"

[build]
buildCommand = "pip install -r requirements.txt"
createDir = "./backend"

[deploy]
startCommand = "cd backend && python main.py"
